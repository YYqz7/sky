<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <!--修改菜品-->
    <update id="update">
        update dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="modifyDishStatus">
        update dish set status = #{status} where id = #{id}
    </update>

    <!--    批量删除菜品-->
    <delete id="deleteBatch">
        delete from dish where id in
        <foreach collection="ids" item="id" separator="，" open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select t1.*, t2.name categoryName
        from dish t1,
        category t2
        where t1.category_id = t2.id
        <if test="name != null">
            and t1.name like concat('%', #{name},'%')
        </if>
        <if test="status != null">
            and t1.status = #{status}
        </if>
        <if test="categoryId != null">
            and t1.category_id = #{categoryId}
        </if>
        order by t1.update_time desc
    </select>

    <!--    根据id查询菜品-->
    <select id="selectByID" resultType="com.sky.entity.Dish">
        select *
        from dish
        where id = #{dishId}
    </select>


    <select id="getDishesByID" resultType="com.sky.entity.Dish">
        select * from dish where category_id = #{categoryId}
    </select>


    <select id="select" resultType="com.sky.entity.Dish">
        SELECT
        id, name, category_id, price, image, description,
        status, create_time, update_time, create_user, update_user
        FROM dish
        <where>
            <!-- 1. 菜品分类ID（非空则作为条件） -->
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <!-- 2. 菜品状态（非空则作为条件，如查询起售中：status=1） -->
            <if test="status != null">
                AND status = #{status}
            </if>
            <!-- 3. 可选：其他属性（根据业务需求扩展，如按名称模糊查询、价格筛选等） -->
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="price != null">
                AND price = #{price}
            </if>
            <if test="description != null and description != ''">
                AND description LIKE CONCAT('%', #{description}, '%')
            </if>
        </where>
        <!-- 默认排序：按创建时间倒序（最新菜品在前） -->
        ORDER BY create_time DESC
    </select>




    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from dish
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>

</mapper>